# TQQQ ETF Feed

This repository generates an RSS2 feed for the TQQQ ETF using yfinance.

Run the generator locally with:

```bash
python fetch_tqqq_feed.py
```

The script writes `docs/feed.rss` and `docs/index.html`. If you want an icon
on GitHub Pages, place `TQQQ.png` in the repository root.
